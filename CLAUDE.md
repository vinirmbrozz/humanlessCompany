# Paperclip

As instruções deste repositório têm **uma fonte única**: [`AGENTS.md`](AGENTS.md) — o padrão
cross-agent (lido nativamente pelo Codex e outros). Este arquivo existe só para o **Claude Code**
carregar o mesmo conteúdo, via o import abaixo. Não edite instruções aqui; edite o `AGENTS.md`.

@AGENTS.md
